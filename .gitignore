__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
*.egg-info/
dist/
build/
.pytest_cache/

# Local tooling scratch. Untracked already, but named explicitly so a
# directory-source plugin install does not sweep them in, and so they can
# never be committed by accident.
.superpowers/
.serena/
tests/.last-transcript.txt
